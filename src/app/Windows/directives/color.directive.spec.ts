import {ColorDirective} from '../../showcase/directives/color.directive';

describe('ColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorDirective();
    expect(directive).toBeTruthy();
  });
});
